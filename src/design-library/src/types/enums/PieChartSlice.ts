export enum PieChartSlice {
  Text = 'text',
  Value = 'value',
  Formatter = 'formatter',
  Color = 'color',
}

export enum PieChartLabelPosition {
  Adjacent,
  Above,
}

export enum PieChartLabelUnitType {
  Number,
  Percent,
  Currency,
}
